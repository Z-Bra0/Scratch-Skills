[project]
name = "scratchai"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "pyyaml>=6.0,<7.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0,<9.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
